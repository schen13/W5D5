class Game {
    run() {
      // until first tower is empty and
      // tower 2 or 3 has all plates in order
        // ask player which tower to get a plate from
        // ask player where to move the plate
        // check if valid move or re-prompt
        // move plate
        // infinite loop
    }
  }
